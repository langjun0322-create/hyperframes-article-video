interface:
  display_name: "Article Video Builder"
  short_description: "文章拆解成配音短视频"
  default_prompt: "Use $hyperframes-article-video to turn this Markdown article into a narrated HyperFrames video with theme-matched branding."
policy:
  allow_implicit_invocation: true
